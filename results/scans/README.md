# Physical Scans

Controlled scan packages live here.

Recommended first scans:

```text
density_scan/
energy_scan/
spin_scan/
inclination_scan/
gbw_iim_comparison/
```

Do not start large scans until the corresponding validation package has passed
and the run registration in `results/catalog/` is working for smaller runs.
